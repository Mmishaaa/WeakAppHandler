# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by `npm run codegen` (see codegen.ts) - never hand-edited, regenerated from the
# Gateway's committed schema.graphql. `npm run build` regenerates it via the `prebuild` script.
/src/gql

# Playwright's own run output (traces, screenshots, HTML report) - regenerated by every
# `npm run test:e2e` run, never hand-edited.
/test-results
/playwright-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
