import './App.css'

/**
 * Root of the feature-sliced application (TASK-033). Deliberately empty for now - the acceptance
 * criterion is that this scaffold builds, lints and generates GraphQL types cleanly; the actual
 * screens are later tasks (TASK-034 onward) under `features/`.
 */
function App() {
  return <div className="app" />
}

export default App
