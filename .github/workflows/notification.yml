name: Notification CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/Notification/**'
      - 'tests/WeakAppHandler.Notification.Api.Tests/**'
      - 'tests/WeakAppHandler.Notification.RuleEngine.Tests/**'
      - 'src/Auth/**'
      - 'src/ServiceDefaults/**'
      - 'src/SharedKernel/**'
      - 'src/Contracts/**'
      - 'tests/Shared/WeakAppHandler.IntegrationTesting/**'
      - 'notification.slnf'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.editorconfig'
      - 'stylecop.json'
      - '.github/workflows/notification.yml'
  push:
    branches: [main]
    paths:
      - 'src/Notification/**'
      - 'tests/WeakAppHandler.Notification.Api.Tests/**'
      - 'tests/WeakAppHandler.Notification.RuleEngine.Tests/**'
      - 'src/Auth/**'
      - 'src/ServiceDefaults/**'
      - 'src/SharedKernel/**'
      - 'src/Contracts/**'
      - 'tests/Shared/WeakAppHandler.IntegrationTesting/**'
      - 'notification.slnf'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.editorconfig'
      - 'stylecop.json'
      - '.github/workflows/notification.yml'

concurrency:
  group: notification-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build, test, and package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore notification.slnf

      # Directory.Build.props sets TreatWarningsAsErrors + StyleCop/.NET analyzers globally, so this
      # build step is also the lint/static-analysis gate - a warning here fails the workflow.
      - name: Build (warnings as errors)
        run: dotnet build notification.slnf --configuration Release --no-restore

      - name: Test
        run: dotnet test notification.slnf --configuration Release --no-build --logger "console;verbosity=normal"

      - name: Build Docker image
        run: docker build -f src/Notification/WeakAppHandler.Notification.Api/Dockerfile -t weakapphandler-notification:ci .
