{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Auth": "Host=localhost;Port=5432;Database=weakapphandler;Username=auth_rw;Password=auth_password"
  },
  "Auth": {
    "Tokens": {
      "AccessTokenLifetimeMinutes": 15,
      "RefreshTokenLifetimeDays": 7
    }
  },
  "Cors": {
    "//": "TASK-042: the frontend's Vite dev-server origin. /login and /refresh are called with credentials (the refresh token is an httpOnly cookie), which is why the shared policy allows credentials. TASK-047 replaces this with the compose frontend address.",
    "FrontendOrigin": "http://localhost:5173"
  }
}
